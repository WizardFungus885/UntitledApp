<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_signup"
        android:orderInCategory="100"
        android:title="@string/action_signup"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_logon"
        android:orderInCategory="100"
        android:title="@string/action_logon"
        app:showAsAction="ifRoom" />

    <!--    The above code for the 2 items should only be visible if the user has not logged/signed up yet-->

    <item android:id="@+id/action_search"
        android:title="@string/action_search"
        android:icon="@drawable/ic_baseline_search_24"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="android.widget.SearchView" />


</menu>